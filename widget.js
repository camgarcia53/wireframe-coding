$(document).ready(function() {
  var url="../data/employees.json"
	$.getJSON(url, function (response));
		var statusHTML = '<ul class="bulleted">';
	});

});




$.each(aarray, function(index, value)



function workLoad {

$.ajaxSetup ( cache: false));
}